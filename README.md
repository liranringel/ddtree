# Website

Standalone Astro site for the DDTree project.

The directory is self-contained: chart data lives in `src/data/results.tex`, and the website assets it serves are committed under `public/assets/project/`.

## Local development

```bash
cd website
npm install
npm run dev
```

## Build

```bash
cd website
npm run build
```
